# C++ HTTP 服务器 · 性能基准测试

> 项目：基于 epoll + 线程池的高性能 HTTP 服务器（`~/test1/http`）
> 用途：性能优化过程留档，数据可直接用于简历量化描述

## 环境

| 项 | 值 |
|---|---|
| 虚拟机 | VMware，Linux，2 核 / 2 worker |
| 编译 | `g++ -Wall -O2 -pthread -std=c++17` |
| 压测客户端 | Python 自研（socket 多线程），全程同一客户端，纵向对比有效 |
| 测试路径 | `/DefaultPage.html`（923 B；V1~V4 时期为 763 B） |
| 网络 | 回环 127.0.0.1，排除网络干扰 |

每次压测前重启服务器，预热 10 次请求。

## 版本记录

### V1 · 阻塞 accept + 每连接一线程（75d2bb3）

主线程阻塞 accept，每连接一个 detach 线程，响应 `Connection: close`。

| | 顺序300 | 100并发 | 32×25 |
|---|---|---|---|
| QPS | 6093 | 4605 | 773 |
| 平均 | 0.164 ms | 0.380 ms | 3.76 ms |
| P99 | 1.16 ms | 1.63 ms | 4.33 ms |

⚠️ 多轮复测 QPS 在 619~12970 间波动，最差 P99 达 **1227 ms**（见发现 1）。

### V2 · 引入线程池组件（2371467）

新增 `ThreadPool` 类（mutex + condition_variable + 任务队列），但未接入主循环，仍为每连接一线程。

| | 顺序300 | 100并发 | 32×25 |
|---|---|---|---|
| QPS | 5596 | 4970 | 746 |
| 平均 | 0.179 ms | 0.364 ms | 23.1 ms |
| P99 | 1.87 ms | 0.92 ms | 1033 ms |

注：该提交有两处笔误（`std:queue` 少一个冒号、lambda 缺分号）导致无法编译，压测在修复副本上进行。

### V3 · epoll 管理监听 socket + 线程池接入（754160e）

ET 模式管理监听 fd，非阻塞 accept 循环一次性收割全部待处理连接，任务提交 2 worker 线程池。

| | 顺序300 | 100并发 | 32×25 |
|---|---|---|---|
| QPS | 18694 | 5466 | **13988** |
| 平均 | 0.053 ms | 0.244 ms | 0.513 ms |
| P99 | 0.37 ms | 1.11 ms | 3.87 ms |

混合并发 QPS 较 V1 提升约 **18 倍**，长尾消失，多轮稳定。

### V4 · keep-alive 长连接（c7dc078）

连接内循环解析请求（缓冲区按 `\r\n\r\n` 切分），连接空闲 30 s 超时关闭。

| | 顺序300 | 100并发 | 32×25 |
|---|---|---|---|
| QPS | 8487 | 5276 | **17247** |
| 平均 | 0.118 ms | 0.345 ms | 0.540 ms |
| P99 | 0.60 ms | 1.00 ms | 21.5 ms |

QPS 较 V3 再提升 **23%**；P99 恶化到 21.5 ms —— 空闲连接占满 worker 所致（见发现 2、3）。

### V5 · 完整 Reactor 事件驱动（98442f0，竞态修复前）

epoll 同时管理监听 fd 与所有连接 fd（LT），worker 按事件处理单次可读，空闲连接不再占用线程。

| | 顺序300 | 100并发 | 32×25 |
|---|---|---|---|
| QPS | 5510 | 4356 | 14863 |
| P99 | 1.29 ms | 1.44 ms | 2.23 ms |
| 失败 | 16/300 ⚠️ | 6/100 ⚠️ | 15/800 ⚠️ |

空闲连接阻塞消除：3 条空闲长连接挂起时，新请求延迟 **0.21~0.26 ms**（V4 同场景 **29.8 s**）。短连接出现 RST，原因见发现 4。

### V5 正式版（e93c16f，2026-09-09，3 轮中值）

在 6170de6（EPOLLONESHOT 竞态修复）之上，另含 close 语义修复、空连接回收、半发数据丢弃、优雅退出。

| | 顺序300 | 100并发 | 32×25（keep） |
|---|---|---|---|
| QPS | 11298.5 | 5349.7 | **33545.7** |
| P50 | 0.068 ms | — | 0.044 ms |
| P99 | 0.389 ms | 0.753 ms | 0.413 ms |
| 失败 | 0 | 0 | 0 |

close 模式单轮参照：8539.8 / 5932.9 / 12793.9（P99 4.564 ms）。

### V6 · LRU 文件缓存（b3e2566，2026-09-15，6 轮中值）

新增 `FileCache`（LRU 淘汰 + 字节上限），命中直接返回内存内容；Makefile 改为 `wildcard` 分步编译。

| | 顺序300 | 100并发 | 32×25（keep） |
|---|---|---|---|
| QPS | 11195.9 | 6071.8 | 30966.9 |
| 平均 | 0.089 ms | 0.222 ms | 0.089 ms |
| P50 | 0.066 ms | — | 0.060 ms |
| P99 | 0.532 ms | 0.545 ms | 0.530 ms |
| 失败 | 0 | 0 | 0 |

close 模式 3 轮中值：10060.1 / 5838.4 / 13393.0（P50 0.293、P99 2.897 ms），失败 0。

**FileCache 在 QPS 上测不出增益**，见发现 5。

### V7 · 语义修正（d8dea60，2026-09-15，6 轮中值）

缓存 mtime 精度升至纳秒、区分 HTTP/1.0 与 1.1 的默认连接方式、头字段名大小写不敏感、发送失败逐层上报。

| | 顺序300 | 100并发 | 32×25（keep） |
|---|---|---|---|
| QPS | 11705.4 | 4406.8 | 25199.2 |
| 平均 | 0.085 ms | 0.353 ms | 0.132 ms |
| P50 | 0.071 ms | — | 0.086 ms |
| P99 | 0.337 ms | 1.132 ms | 0.959 ms |
| 失败 | 0 | 0 | 0 |

close 模式 3 轮中值：11118.5 / 5879.9 / 13274.6（P50 0.223、P99 4.038 ms），失败 0。

⚠️ 本段为口径 A（32×25，约 0.03 s 窗口）数据，绝对值系统偏低。原记录的「较 V6 下降约 19%」**已经复测证伪** —— 放大测量窗口后两版无显著差异，见发现 6。

## 结果总览（简历引用）

| 版本 | 混合并发 QPS | P99 | 稳定性 |
|---|---|---|---|
| V1 阻塞 + 每连接一线程 | 773 | 4.3 ms（最差 1227 ms） | 差 |
| V2 +线程池组件 | 746 | 1033 ms | 差 |
| V3 +epoll 监听 | 13988（**18×**） | 3.87 ms | 稳定 |
| V4 +keep-alive | 17247（**22×**） | 21.5 ms | 稳定 |
| V5 完整 Reactor（修复前） | 14863 | 2.23 ms | 短连接有 RST |
| V5 正式（e93c16f） | **33546** | 0.413 ms | 全场景失败 0 |
| V6 +LRU 缓存（b3e2566） | 30967 | 0.530 ms | 全场景失败 0 |
| V7 语义修正（d8dea60） | 25199 | 0.959 ms | 全场景失败 0 |

> 上表全部为口径 A 数据（测量窗口约 0.03 s），**绝对值系统偏低**，版本间的相对提升仍有效。其中 V6 与 V7 之间的「下降」为测量噪声：放大窗口复测后两版无显著差异（46,253 vs 46,958，区间重叠），详见发现 6。

> 简历口径建议：*"空闲连接阻塞从 29.8 s 降到 0.24 ms，P99 从 21.5 ms 降到 0.4 ms；压测暴露 LT 重复派发竞态，定位后以 EPOLLONESHOT 修复，失败归零；最终版混合长连接 QPS 3 万+、全场景零失败。"*

> 若被追问数字口径，可补充：口径 B（放大窗口）下最终版混合长连接 QPS 为 **4.6 万+**，该数值有 5 轮区间与同代码对照记录支撑（见发现 6）。

## 测试方法

分两套口径：口径 A 用于 V1~V7 的版本阶梯记录；口径 B 于 2026-09-16 引入，用于判定版本间是否存在真实差异。

### 口径 A · 原始窗口（V1~V7 沿用）

| 场景 | 说明 |
|---|---|
| 顺序短连接 | 300 个请求逐个发送，每个请求新建 TCP 连接 |
| 100 并发突发 | 100 个连接同时建立，各发 1 个请求后关闭 |
| 32×25 混合 | 32 个并发连接 × 各 25 个请求（close / keep 两种模式） |

脚本：`bench/bench.py`。整套跑完**不足 0.1 s**，线程创建、连接建立等固定开销与测量量同量级，绝对值被低估，短连接场景不可复现。

### 口径 B · 放大窗口（2026-09-16 引入）

| 场景 | 规模（keep / close） | 测量窗口 |
|---|---|---|
| 顺序短连接 | 3000 / 2000 | ~0.3 s |
| 并发突发 | 100 × 20 / 100 × 10 | ~0.4 s |
| 32×N 混合 | 32 × 1000 / 32 × 100 | ~0.7 s |

脚本：`bench/bench2.py`（由 `bench.py` 参数化而来，规模经环境变量注入，其余逻辑完全一致）。连接被 RST 记为失败并继续，失败数单独统计。

## 关键发现

### 发现 1 · 阻塞 accept + 小 backlog 导致约 1 s 长尾（V1/V2）

主循环每次 accept 只取一个连接，监听队列（backlog=10）在突发下溢出，客户端触发 SYN 重传（~1 s）。
→ V3 改 epoll + 非阻塞 accept 循环一次性收割，长尾消失。

### 发现 2 · 长连接占满线程池（V4，实测复现）

2 条空闲 keep-alive 连接占满 2 个 worker 后，第 3 条连接的请求延迟 **29.8 s**（卡到 30 s 空闲超时才被处理）。
原因：epoll 只管理监听 socket，已接入连接仍由 worker 阻塞式 `recv` 独占。
→ V5 改为完整 Reactor，按事件分发而非按连接占用线程。

### 发现 3 · keep-alive 的收益与代价（V4）

收益：省去每请求握手挥手，混合 QPS +23%，P50 从 0.32 ms 降至 0.013 ms。
代价：阻塞 worker 模型下空闲连接直接侵蚀并发容量，P99 恶化至 21.5 ms。
结论：**keep-alive 必须与事件驱动模型配合，否则得不偿失。**

### 发现 4 · LT 模式重复派发竞态（V5 暴露，已修复闭环）

现象：短连接压测中客户端大量收到 `Connection reset by peer`（失败率 5%~35%），服务器日志无任何异常。
复现条件：连接高速建断，fd 号被内核快速复用时必现。
原因：LT 模式下 worker 尚未 `recv` 时 epoll_wait 会再次上报同一 fd，主线程派发第二个 worker；两个 worker 并发操作同一 fd → double close，或误关已复用给新连接的 fd → 新连接被 RST。
修复：注册 `EPOLLIN | EPOLLONESHOT`，处理期间 epoll 哑火；`handleOnce` 收尾后用 `EPOLL_CTL_MOD` 重新武装，保证同一 fd 任意时刻最多一个 worker。

| 场景 | 修复前失败 | 修复后失败 | 修复后 QPS |
|---|---|---|---|
| 顺序 300 | 16/300 | **0/300** | 5405 |
| 突发 100 | 6/100 | **0/100** | 5371 |
| 混合 32×25 | 108/800 | **0/800** | 8743 |

教训：**失败率本身就是指标**。加固压测客户端统计 RST 后，竞态从"偶现玄学"变成可量化数据；定位 → 量化 → 修复 → 复测归零的完整闭环，比一帆风顺的数据更有说服力。

### 发现 5 · FileCache 在 QPS 上测不出增益（V6）

现象：引入 LRU 文件缓存后，混合 QPS 为 30967，与 V5 正式版（33546）同量级，差异落在单轮波动范围内。
原因：923 B 文件预热后完全命中内核页缓存，`read()` 已接近内存拷贝速度，缓存只省下一次 `open` + `read` 系统调用。
结论：**FileCache 的价值在大文件与磁盘 IO 竞争场景，当前测试口径覆盖不到。** 测出没提升并解释清楚原因，比编一个数字更有价值。

### 发现 6 · 短窗口测量造成的假差异（V7，已证伪）

**原结论（2026-09-15，口径 A）**：V7 长连接混合 QPS 较 V6 下降约 19%（25199 vs 30967，各 6 轮区间不重叠），推测源于 `clientWantsClose` 每请求新增 2 次堆分配。

**复测（2026-09-16，口径 B）**：放大测量窗口至约 0.7 s，三版本交替执行、顺序逐轮反转、各 5 轮：

| 版本 | 中值 QPS | CV | 5 轮区间 |
|---|---|---|---|
| V6 `b3e2566` | 46,252.8 | 3.6% | 43,243 ~ 47,057 |
| V7 `d8dea60` | 46,958.1 | 4.2% | 43,709 ~ 47,700 |
| A4 工作区（与 V7 同代码） | 44,290.3 | 6.7% | 38,964 ~ 46,773 |

- V6 与 V7 相差 **+1.5%**，区间大幅重叠 → **无显著差异**，原结论证伪。
- A4 与 V7 是**同一份代码的不同构建**（仅拆分编译单元），却相差 **-5.7%**。这个数字就是本环境的**噪声本底** —— 比 V6/V7 之间的差异还大。可见口径 A 下「两组区间不重叠」不足以支撑结论。

**根因**：口径 A 的测量窗口只有约 0.03 s，进程调度抖动、后台负载的瞬时影响都能造成 20% 以上的偏差。放大到 0.7 s 后，keep 混合场景的 CV 降到 3.6%~6.7%。

**教训**：① 测量窗口必须显著长于被测系统的时间尺度，否则会把噪声当信号；② 判定版本差异时需要一个「同代码对照」来量化噪声本底 —— 没有本底，就无法判断差异是否可信。

## 备注

- 单轮 QPS 波动可达 ±20%~30%，**任何小于一轮波动的版本间差异不构成结论**；结论只取多轮中值方向一致且幅度显著者。V6 与 V7 为各 6 轮中值，V5 正式版为 3 轮中值，V1~V4 为单轮或多轮中值。
- **口径 B 下判定版本差异的三条门槛**：① 区间不重叠；② 幅度大于同代码对照（噪声本底）；③ 多轮方向一致。V6 vs V7 三条均不满足，判为无差异。
- 口径 B 下**顺序短连接场景仍不可用**（各版本 CV 21%~30%，个别轮次相差 80%），只有 keep 长连接的 32×N 并发场景能给出可信数字（CV 3.6%~6.7%）。
- 测试时间：V1~V4 为 2026-09-02，V5 正式版为 2026-09-09，V6 与 V7 为 2026-09-15，口径 B 复测为 2026-09-16。
- 版本对比通过 `git worktree` 检出独立构建，统一监听 8080 串行压测；口径 B 额外采用三版本交替执行、顺序逐轮反转。
- V2 提交源码的两处笔误已在压测副本修复，不影响其他版本。
