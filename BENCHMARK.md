# C++ HTTP 服务器性能基准测试记录

> 项目：基于 epoll + 线程池的高性能 HTTP 服务器（`~/test1/http`）
> 用途：性能优化过程留档，数据可直接用于简历量化描述

## 环境信息

| 项目 | 值 |
|---|---|
| 虚拟机 | VMware，Linux，2 核 / 2 线程池 worker |
| 编译选项 | g++ -Wall -O2 -pthread -std=c++17 |
| 测试工具 | Python 自研压测客户端（socket 多线程） |
| 测试路径 | 短连接版本：`/`（内置默认页）；v3/v4：`/DefaultPage.html`（763 B 文件） |
| 网络 | 本地回环 127.0.0.1，排除网络干扰 |
| 测试时间 | 2026-09-02 |

> 说明：压测客户端为 Python，QPS 绝对值受客户端性能限制；各版本使用同一客户端，**纵向对比有效**。每次压测前重启服务器。

## 版本记录

### 版本 1：阻塞 accept + 每连接一线程（commit 75d2bb3）

主线程阻塞 accept，为每个连接创建独立线程（detach），响应 `Connection: close`。

| 指标 | 顺序请求×300 | 100 并发短连接 | 32 并发×25 请求 |
|---|---|---|---|
| QPS | 6093 | 4605 | 773 |
| 平均延迟 | 0.164 ms | 0.380 ms | 3.76 ms |
| P99 延迟 | 1.16 ms | 1.63 ms | 4.33 ms |

- 测试条件：新建连接/请求，客户端主动关闭
- ⚠️ 多轮复测 QPS 在 619~12970 间大幅波动，P99 最差达 **1227 ms**（见关键发现 1）

### 版本 2：引入线程池组件（commit 2371467）

该提交新增 `ThreadPool` 类（mutex + condition_variable + 任务队列），但主循环尚未接入，仍为每连接一线程。

| 指标 | 顺序请求×300 | 100 并发短连接 | 32 并发×25 请求 |
|---|---|---|---|
| QPS | 5596 | 4970 | 746（3 轮中值：751） |
| 平均延迟 | 0.179 ms | 0.364 ms | 23.1 ms |
| P99 延迟 | 1.87 ms | 0.92 ms | 1033 ms |

- 测试条件：同版本 1
- 注：该提交源码存在两处笔误（`std:queue` 少一个冒号、lambda 缺分号）导致无法编译，压测副本中修复后编译通过

### 版本 3：epoll 管理监听 socket + 线程池接入（commit 754160e）

epoll 边沿触发管理监听 socket，非阻塞 accept 循环一次性收割所有待处理连接，连接任务提交线程池（2 worker）处理，响应 `Connection: close`。

| 指标 | 顺序请求×300 | 100 并发短连接 | 32 并发×25 请求 |
|---|---|---|---|
| QPS | **18694** | 5466 | **13988** |
| 平均延迟 | 0.053 ms | 0.244 ms | 0.513 ms |
| P99 延迟 | 0.37 ms | 1.11 ms | 3.87 ms |

- 测试条件：同版本 1
- 对比版本 1：混合并发 QPS 提升约 **18 倍**（773 → 13988），P99 从 4.3 ms（最优轮次）持平，且**多轮结果稳定，长尾消失**

### 版本 4：keep-alive 长连接（当前工作区，未提交）

在版本 3 基础上支持 `Connection: keep-alive`：连接内循环解析请求（缓冲区按 `\r\n\r\n` 切分），连接空闲 30 s 超时关闭。

| 指标 | 顺序短连接×300 | 100 并发短连接 | 32 并发×25 请求（长连接） |
|---|---|---|---|
| QPS | 8487 | 5276 | **17247** |
| 平均延迟 | 0.118 ms | 0.345 ms | 0.540 ms |
| P99 延迟 | 0.60 ms | 1.00 ms | 21.5 ms |

- 测试条件：混合场景下 32 条连接复用，各发 25 个请求
- 长连接复用使混合并发 QPS 较版本 3 再提升 **23%**（13988 → 17247）
- P99 = 21.5 ms：空闲/排队连接被少量 worker 阻塞所致，见关键发现 2

## 结果总览（简历引用）

| 版本 | 混合并发 QPS | P99 延迟 | 稳定性 |
|---|---|---|---|
| V1 阻塞 + 每连接一线程 | 773 | 4.3 ms（不稳定，最差 1227 ms） | 差 |
| V2 +线程池组件 | 746 | 1033 ms | 差 |
| V3 +epoll 监听 | 13988（**18×**） | 3.87 ms | 稳定 |
| V4 +keep-alive | 17247（**22×**） | 21.5 ms | 稳定 |

## 测试方法

### 顺序短连接
- 300 个请求逐个发送，每个请求新建 TCP 连接，测 QPS / 延迟分布

### 100 并发短连接
- 同时建立 100 个连接，各发 1 个请求后关闭，测成功率与 QPS

### 混合并发压测
- 32 个并发连接 × 各 25 个请求（短连接或 keep-alive 两种模式），统计 QPS、平均延迟、P50/P99

### 空闲连接阻塞实验（针对 V4）
- 先建立 2 条空闲长连接占满 2 个 worker，再发第 3 条连接的请求，测其响应延迟

## 关键发现记录

### 发现 1：阻塞 accept + 小 backlog 导致 ~1s 长尾
- 现象：V1/V2 在突发并发下 QPS 波动大（619~12970），约 1% 请求延迟 ~1000 ms
- 原因：主循环每次 accept 仅取一个连接，监听队列（backlog=10）在突发下溢出，客户端触发 SYN 重传（~1s）
- 解决：epoll + 非阻塞 accept 循环一次性收割全部连接（V3），长尾消失，多轮测试稳定

### 发现 2：长连接占满线程池（实测复现）
- 现象：2 条空闲 keep-alive 连接占满 2 个 worker 后，第 3 条连接的请求延迟 **29.8 s**（卡到 30 s 空闲超时才被处理）
- 原因：epoll 仅管理监听 socket，已接入连接仍由 worker 阻塞式 `recv` 独占，空闲连接白白占用线程
- 解决方向：完整 Reactor（epoll 同时管理连接 fd 的读写事件，按事件分发请求而非按连接占用线程），或将空闲超时降至秒级

### 发现 3：keep-alive 的收益与代价
- 收益：省去每请求握手/挥手，混合并发 QPS +23%，P50 从 0.32 ms 降至 0.013 ms
- 代价：在"阻塞 worker 处理连接"的模型下，空闲连接直接侵蚀并发容量（发现 2），P99 恶化至 21.5 ms
- 结论：keep-alive 必须与事件驱动模型配合，否则得不偿失

## 备注
- 每次测试前重启服务器，预热 10 个请求
- V2 提交源码的两处笔误已在压测副本修复，不影响其他版本
- 压测脚本：虚拟机 `/tmp/bench.py`（Python 标准库实现，支持短连接/keep-alive 两种模式与延迟分位统计）
